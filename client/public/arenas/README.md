# Arenas

Drop level JSON exports here. The game currently loads:

**`grassareana.json`**

After replacing the file, restart the server and refresh the client.

Edit in `/editor.html`, then **Download** and overwrite this file.
